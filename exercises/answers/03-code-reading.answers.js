'use strict';

/**
 * ТАСК 03 — читаємо код, нічого не запускаючи.
 *
 * Тут не треба ні гуглити, ні запускати сервер. Відкрий свій index.js,
 * знайди потрібний шматок, прочитай його і подумай.
 *
 * Впиши літеру відповіді замість null: 'a', 'b' або 'c'.
 *
 * Потім запусти:  node exercises/run.js 03
 */

module.exports = {
  /**
   * q1. У твоєму app.put('/users/:id') є такий рядок:
   *
   *       if (name) user.name = name;
   *
   *     Уяви: користувач хоче стерти своє ім'я і надсилає
   *     PUT /users/1 з тілом { "name": "" } (порожній текст).
   *
   *     Що станеться з іменем?
   *
   *     a) стане порожнім — тобто так, як він і просив
   *     b) не зміниться взагалі, залишиться старе
   *     c) сервер відповість 400
   */
  q1: null,

  /**
   * q2. Уяви, що ти випадково видалила з файлу рядок:
   *
   *       app.use(express.json());
   *
   *     Все інше залишилось як було. Хтось надсилає нормальний
   *     POST /users з тілом { "name": "Anna", "email": "anna@test.com" }.
   *
   *     Що відповість сервер?
   *
   *     a) все спрацює як раніше — express і сам розбирає JSON
   *     b) 400 "name and email are required"
   *     c) 500 — тобто помилка на стороні сервера
   */
  q2: null,

  /**
   * q3. Уяви, що ти дописала ще один endpoint — НИЖЧЕ за той, що вже є:
   *
   *       app.get('/users/:id', ...)   // цей у тебе вже написаний
   *       app.get('/users/new', ...)   // а цей ти додала під ним
   *
   *     Хтось відкриває GET /users/new. Який із двох handler'ів спрацює?
   *
   *     a) новий, /users/new
   *     b) старий, /users/:id
   *     c) ніякий, буде помилка 500
   */
  q3: null,

  /**
   * q4. У DELETE ти пишеш res.status(204).send(), а не res.json({...}).
   *     Чому саме так?
   *
   *     a) 204 означає "все вийшло, але показувати нема чого" — тому й порожньо
   *     b) res.json() працює повільніше
   *     c) різниці немає, просто так вийшло
   */
  q4: null,

  /**
   * q5. Після POST /users виконується рядок users.push(newUser).
   *     Де фізично опиняється цей новий користувач?
   *
   *     a) у файлі на диску, поруч з index.js
   *     b) в оперативній пам'яті — там, де зараз працює запущений node
   *     c) у браузері того, хто зробив запит
   */
  q5: null,
};
